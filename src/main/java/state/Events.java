package state;

public enum Events {
    //un enum che tiene i due tipi di eventi che possono verificarsi, il login nel caso l'utente ha effettuato il login
    //il logout nel caso ha invece effettuato il logout
    LOGIN,LOGOUT
}
